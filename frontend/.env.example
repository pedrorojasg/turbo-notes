# Copy to .env.local for local development.

# Base URL of the Django backend the BFF route handlers and Server Components
# proxy to. Local Docker default below; in production set this to the deployed
# Railway backend URL (e.g. https://turbo-notes-api.up.railway.app).
NEXT_PUBLIC_API_URL=http://localhost:8000
